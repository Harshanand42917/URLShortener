<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>URL Shortener Project</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet" />
    <style>
      body {
        font-family: "Poppins", sans-serif;
        margin: 0;
        padding: 0;
        background: radial-gradient(circle at top left, #ff758c, #ff7eb3);
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        overflow: hidden;
      }

      .container {
        background: rgba(26, 26, 46, 0.8);
        border: 1px solid rgba(255, 255, 255, 0.2);
        backdrop-filter: blur(15px);
        -webkit-backdrop-filter: blur(15px);
        padding: 50px 40px;
        border-radius: 20px;
        box-shadow: 0 15px 30px rgba(0, 0, 0, 0.5), 0 0 35px rgba(255, 105, 180, 0.4);
        text-align: center;
        width: 420px;
        animation: fadeIn 1.3s ease;
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: scale(0.95);
        }
        to {
          opacity: 1;
          transform: scale(1);
        }
      }

      h1 {
        font-size: 36px;
        background: linear-gradient(90deg, #ff6f91, #ff9eac);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        margin-bottom: 30px;
      }

      label {
        font-size: 18px;
        display: block;
        color: #ffffffcc;
        margin-bottom: 10px;
      }

      input[type="text"] {
        width: 100%;
        padding: 15px;
        border: none;
        border-radius: 10px;
        background: #2f2f55;
        color: #fff;
        font-size: 16px;
        margin-bottom: 25px;
        transition: 0.3s;
      }

      input[type="text"]:focus {
        outline: none;
        background: #3f3f65;
        box-shadow: 0 0 10px rgba(255, 182, 193, 0.6);
      }

      button {
        padding: 14px 28px;
        border: none;
        border-radius: 10px;
        background: linear-gradient(135deg, #ff758c, #ff9eac);
        color: white;
        font-weight: 600;
        font-size: 16px;
        cursor: pointer;
        transition: transform 0.2s ease-in-out, box-shadow 0.3s ease;
      }

      button:hover {
        transform: translateY(-3px);
        box-shadow: 0 8px 20px rgba(255, 182, 193, 0.5);
      }

      p {
        margin-top: 20px;
        font-size: 16px;
        color: #ffffff;
      }

      a {
        color: #ffddc1;
        text-decoration: none;
        font-weight: bold;
        word-break: break-word;
      }

      a:hover {
        color: #ffe4e1;
        text-decoration: underline;
      }

      .result-icon {
        font-size: 30px;
        color: #ffddc1;
        margin-bottom: 10px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>🔗 Shorten Your URL</h1>
      <form action="/short" method="POST">
        <label for="longUrl">Paste your long URL here:</label>
        <input type="text" id="longUrl" name="longUrl" placeholder="https://example.com/very-long-url" required />
        <button type="submit">✨ Shorten</button>
      </form>

      <% if(shortUrl) { %>
      <div style="margin-top: 30px;">
        <div class="result-icon">✅</div>
        <p>
          Your short link is:
          <br />
          <a href="<%= shortUrl %>" target="_blank"><%= shortUrl %></a>
        </p>
      </div>
      <% } %>
    </div>
  </body>
</html>
